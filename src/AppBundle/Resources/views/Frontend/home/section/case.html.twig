<div class="window-bin" data-hash="case">
  <div class="window-back swiper-lazy" data-title="案例" data-background="{{ asset('static/images/wp3.jpg','frontend') }}">
    <div class="hidden visible-sm-block visible-md-block">
    <div class="case-ctrl">
      <div class="ctrl-box ctrl-left"><i class="fa fa-angle-left"></i></div>
      <div class="ctrl-box ctrl-right"><i class="fa fa-angle-right"></i></div>
    </div>
    </div>
    <div class="table-box">
      <div class="table-cell">
        <div class="title-box">
          <h1>CASE</h1>
          <h2>案例展示</h2>
        </div>
        <div class="case-box">
          <ul class="case-cut">
            {% for item in cases %}
              <li class="case-bin">
                <a href="{{ path('case_info',{id: item.id}) }}" title="{{ item.title }}">
                  <font>
                    <b></b>
                    <img data-src="{{ parameter('img_prefix') }}{{ item.thumbnail }}" class="swiper-lazy" title="{{ item.title }}" alt="{{ item.title }}">
                  </font>
                  <span>

					  <p>网站设计</p>
					  <h3 style="margin-top: 15px;">{{ item.title }}</h3>
					  <p>套餐价格: &nbsp;&nbsp;&nbsp;{% if item.showPrice %}{{ item.price }}{% else %}***{% endif %}</p>
					</span>
                </a>
              </li>
            {% endfor %}
          </ul>
        </div>
        <div class="case-more"><a href="{{ path('case_list') }}" title="设计案例">更多设计案例</a></div>
      </div>
    </div>
  </div>
</div>
